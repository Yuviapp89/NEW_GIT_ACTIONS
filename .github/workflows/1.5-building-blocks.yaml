# .github/workflows/hello-world.yml

name: 1.5-building-blocks

on: push

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello, World!"

